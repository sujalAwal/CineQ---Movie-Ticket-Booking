<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 
         Read APP_LOG_LEVEL directly from OS environment variable (not Spring property).
         This ensures it works at logback init time, before Spring context is fully loaded.
    -->
    <variable name="LOG_LEVEL" value="${APP_LOG_LEVEL:-INFO}"/>

    <!-- ============================================================
         GLOBAL CONSOLE APPENDER (always active, regardless of profile)
         This ensures logs are ALWAYS written to stdout/stderr.
         ============================================================ -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Global root logger — ALWAYS outputs to CONSOLE -->
    <root level="${LOG_LEVEL}">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Application loggers (always active) -->
    <logger name="com.awal.cineq" level="${LOG_LEVEL}"/>
    <logger name="org.springframework.web" level="${LOG_LEVEL}"/>
    <logger name="org.springframework.security" level="${LOG_LEVEL}"/>
    <logger name="org.springframework.boot" level="${LOG_LEVEL}"/>
    <logger name="org.springframework.data" level="${LOG_LEVEL}"/>

    <!-- LOCAL PROFILE: Additionally log to FILE with DEBUG -->
    <springProfile name="local">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>application.log</file>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>application.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>10MB</maxFileSize>
                <maxHistory>7</maxHistory>
                <totalSizeCap>100MB</totalSizeCap>
            </rollingPolicy>
        </appender>

        <root level="DEBUG">
            <appender-ref ref="FILE"/>
        </root>

        <logger name="com.awal.cineq" level="DEBUG"/>
        <logger name="org.springframework.web" level="DEBUG"/>
        <logger name="org.springframework.security" level="DEBUG"/>
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>
    </springProfile>
</configuration>
