# Production Environment Configuration for Docker
# Set these as environment variables on your production server

# Database Configuration (External PostgreSQL - Aiven)
DB_URL=jdbc:postgresql://your-prod-host:port/database?sslmode=require
DB_USERNAME=your_prod_username
DB_PASSWORD=your_secure_production_password

# Security Configuration
JWT_SECRET=your-very-secure-jwt-secret-key-for-production
JWT_EXPIRATION=86400

# Application Configuration
SPRING_PROFILES_ACTIVE=prod